[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    integration: marks tests that require running external services
    slow: marks tests that are slow (model downloads, etc.)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning:chromadb
